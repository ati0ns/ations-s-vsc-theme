const name = "value";;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
const fn = () => undefined;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
let int = 0;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

''
`${int}`

function run(callback, ...args) {
    return callback(args);
}

(callback, ...args) => callback(args);

return;

(callback, ...args) => callback(args);



if ([].some(e => e)) {
    console.log(true);
} else return null;

switch (int) {
    case 1:
        int++;
        break;
    default:
        int *= 2;
        break;
}



// Single-line comment
/**
 * Multi-line comment
 */



{
    test: Infinity
}
(-6.4);
[1, `2`, three]